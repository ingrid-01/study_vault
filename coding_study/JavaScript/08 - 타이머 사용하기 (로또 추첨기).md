# 1. Fisher-Yates Shuffle Algorithm #

- 숫자를 무작위로 섞는 방법
- 무작위 인덱스를 하나 뽑은 후, 그에 해당되는 요소를 새로운 배열로 옮김
	- 이를 반복 = 새 배열에 무작위로 섞인 숫자들이 들어감
	
``` js
const cadidate = Array(45).fill().map((v, i) => i + 1);
const shuffle = [];
while (candidate.length > 0) {
	 const random = Math.floor(Math.random() * candidate.length);
	 const spliceArray[0];
	 shuffle.push(value);
}
console.log(shuffle);
```

# 2. alert() 대화상자가 앞 동작보다 더 먼저 보이는 문제 #

- 화면을 그리고 나서 alert() 매서드를 호출할 때, 대화상자가 화면을 그리는 동작보다 먼저 보이는 현상 존재
	- 이럴 때:
		- 화면을 그리는 코드와 alert() 매서드 사이에 비동기 함수를 넣으면 됨
```js
drawBall(bonus, $bonus);
await setTimeoutPromise(0);
let count = 0;
```

# 3. 공에 색 넣기 #
`colorize()` = 색 지정 함수

#JS/intro
#JS/prac
#timer
#lotto_number_distributor