- 자바스크립트는 동적 타입 (dynamic type) 언어
	- 변수에 값 할당 =/= 예측

```js
function sum(a, b) {
	return a + b;
}
```
위 코드 의도:
- 2개의 number 타입 인수를 전달받음
- 그 합계를 반환하려는 것

BUT 코드 상:
- 어떤 타입의 인수를 전달하여야 하는지 + 어떤 타입의 값을 반환해야 하는지
- 명확하지 않다

Result:
```js
function sum(a, b) {
	 return a + b;
}

sum('x', 'y'); // 'xy'
```