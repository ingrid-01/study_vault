> Started: 17 June
> Completed: 17 June

* editor panel uses JavaScript
* Changing Variables 
	* variables - store data in a program, can be manipulated & referenced throughout the code
	* code editor - program writing software; human readable

`message = 'Change the message!';`
	↳ `message` = variable
	↳ `Change the message!` = stored text for `message`

* Changing Colours
	* `drawName()` - set of repeatable code = ==function==
		↳ w 2 values (e.g. variable, colour), sets the msg + colour
* Multiple Colours
	* `letterColors` - list of variables inside [], separated by commas
		`letterColors = [red, orange, green, blue, purple];`

- Change animation
	- `mouseResponseThreshold`, `friction`, `rotationForce` 

#Codecademy #CodingBasics 